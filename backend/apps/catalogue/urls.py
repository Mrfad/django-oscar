from django.urls import path


urlpatterns = [
    # path('', home, name='home'),
    # path('store/', store, name='store' ),
    # path('store/<slug:category_slug>/', store, name='product_list_by_category'),
]